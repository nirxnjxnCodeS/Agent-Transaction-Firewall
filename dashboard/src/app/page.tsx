// Decision feed — live list of firewall decisions with approve/reject UI
// for items in ESCALATE state.
export default function DashboardPage() {
  return (
    <main>
      <h1>Agent TX Firewall</h1>
      <p>Decision feed — not yet implemented.</p>
    </main>
  );
}
